<!--
    Cette vue partielle a pour mission de construire l'entête et les menus en fonction de l'action
-->
@{
    string controller = HttpContext.Current.Request.RequestContext.RouteData.Values["controller"].ToString().ToLower();
    string action = HttpContext.Current.Request.RequestContext.RouteData.Values["action"].ToString().ToLower();

    bool search = Session["Search"] != null ? (bool)Session["Search"] : false;
    string searchString = Session["SearchString"] != null ? (string)Session["SearchString"] : "";
    bool SearchAscending = Session["SortAscending"] != null ? (bool)Session["SortAscending"] : false;
    bool course = Session["Course"] != null ? (bool)Session["Course"] : false;
    bool teacher = Session["Teacher"] != null ? (bool)Session["Teacher"] : false;
    bool student = Session["Student"] != null ? (bool)Session["Student"] : false;
    string iconSortBy = "";
    string iconSortByTitle = "";

    bool userConnected = Models.User.ConnectedUser != null;
    bool userIsAdmin = userConnected ? Models.User.ConnectedUser.IsAdmin : false;
    bool userCanWrite = userConnected ? Models.User.ConnectedUser.CanWrite : false;
    string index = userConnected ? "/" + controller + "/List" : "/Accounts/Login";
}

<div class="section roundedBorder">
    <div class="headerMenu">
        <i class="siteIcon"></i>
        <div class="pageTitle"> @ViewBag.PageTitle </div>
        @if(userConnected)
        {
        <div class="mainNav">
            <a href="~/Students/List" class="navItem" @(controller == "students" ? "navActive" : "")>
                Étudiants
            </a>
            <a href="/Teachers/List" class="navItem" @(controller == "teachers" ? "navActive" : "")>
                Enseignants
            </a>
            <a href="/Courses/List" class="navItem" @(controller == "courses" ? "navActive" : "")>
                Cours
            </a>
        </div>
        }
        <!-- Direct action context command icons -->
        @switch (action)
        {
            case "login": break;
            case "subscribe":
                <div id="mainMenuPanel" class="menu">
                    <i class="cmdIcon submitCmd fa fa-circle-check" title="Soumettre" onclick="document.getElementById('submit').click()"></i>
                    <a class="cmdIcon fa fa-square-xmark" title="Annuler" onclick="history.back()"></a>
                </div>
                break;
            case "editprofil":
                <div id="mainMenuPanel" class="menu">
                    <i class="cmdIcon submitCmd fa fa-circle-check" title="Soumettre" onclick="document.getElementById('submit').click()"></i>
                    <a class="cmdIcon fa fa-square-xmark" title="Annuler" onclick="history.back()"></a>
                </div>
                break;
            case "list":
                <div id="mainMenuPanel" class="menu">
                    @if (userCanWrite)
                    {
                        <a href="/@controller/Create" class="cmdIcon fa fa-circle-plus" title="Ajouter..."></a>
                    }
                    <a href="/@controller/ToggleSearch" class="cmdIcon fa fa-search" title="Recherche..."></a>

                    <!-- Dropdown menu -->
                    <div id="menu" class="dropdown ms-auto">
                        <div data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="cmdIcon fa fa-bars" title="Menu"></i>
                        </div>
                        <div class="dropdown-menu noselect" id="DDMenu" style="width:300px !important">
                            <!-- keep all your dropdown items exactly as they are -->
                            ... (no change here)
                        </div>
                    </div>
                </div>
                break;
            case "details":
                <div id="mainMenuPanel" class="menu">
                    <div id="likesPanel">
                        <!-- auto refreshed -->
                    </div>
                    @if (userCanWrite && (bool)ViewBag.IsOwner)
                    {
                        <a href="/@controller/Edit/@ViewBag.Id" class="cmdIcon fa fa-pen-to-square" title="Modifier..."></a>
                        <i class="cmdIcon fa fa-trash-can"
                           title="Effacer"
                           onclick="bootbox.confirm(`<b style=color:red>Effacer cet élément`, function(result){ if (result) window.location = '/@controller/Delete/@ViewBag.Id'; })">
                        </i>
                    }

                    <a href="/@controller/List" class="cmdIcon fa fa-square-xmark" title="retour"></a>
                </div>
                break;
            case "create":
                if (userCanWrite)
                {
                    <div id="mainMenuPanel" class="menu">
                        <i class="cmdIcon submitCmd fa fa-circle-check" title="Soumettre" onclick="document.getElementById('submit').click()"></i>
                        <a href="/@controller/List" class="cmdIcon fa fa-square-xmark" title="Annuler"></a>
                    </div>
                }
                break;
            case "edit":
                <div id="mainMenuPanel" class="menu">
                    <i class="cmdIcon submitCmd fa fa-circle-check" title="Soumettre" onclick="document.getElementById('submit').click()"></i>
                    <a href="/@controller/Details/@ViewBag.Id" class="cmdIcon fa fa-square-xmark" title="Annuler"></a>
                </div>
                break;
            case "loginsjournal":
            case "eventsjournal":
            case "manageusers":
            case "about":
                <div id="mainMenuPanel" class="menu">
                    <a href="/@controller/List" class="cmdIcon fa fa-square-xmark" title="Retour"></a>
                </div>
                break;
        }

    </div>
</div>
@if (search && action == "list")
{
    <div class="searchBar">
        <input type="search" id="searchInput" class="section roundedBorder searchBarItem" value="@searchString" placeholder="Recherche...">
    </div>
}


