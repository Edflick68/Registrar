@model IEnumerable<Models.Course>
@{
    var yearsList = ((List<int>)Session["CourseYearsList"]).OrderByDescending(i => i);
}
@foreach (int year in yearsList)
{
    var courses = Model.Where(s => s.Year == year);
    if (courses.Count() > 0)
    {
        <details id="course_details_@year">
            <summary>Cohorte @year </summary>
            @foreach (var item in courses)
            {
                <div class="StudentListLayout">
                    <a href=" @Url.Action( "Details" , new { id = item .Id })">
                        <div style="display : flex">
                            <span class="courseCode"> @item.Code </span>
                            <div class="cName"> @item.LastName @item.FirstName </div>
                        </div>
                    </a>
                </div>
            }
        </details>
    }
}